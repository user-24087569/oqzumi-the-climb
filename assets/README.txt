Drop your app icon here as icon.png (1024x1024) before running `expo build`.
The mascot from the prototype (gradient teal-to-violet rounded body, simple
robot face) makes a strong starting point for the icon design.
